# /opt/MigrationTools/migrate_common.ph

$DEFAULT_MAIL_DOMAIN = "example.com";
$DEFAULT_BASE = "dc=example,dc=com";

$DEFAULT_PASSWD_FILE = "/etc/passwd";
$DEFAULT_GROUP_FILE = "/etc/group";
$EXTENDED_SCHEMA = 1;

